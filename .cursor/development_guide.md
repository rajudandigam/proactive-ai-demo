/**
 * Proactive AI Demo V2 — see docs/demo-v2-brief.md for the full specification.
 * Stage default: LIVE OpenAI + mock travel facts + preview delivery.
 * Fixture/replay remain explicitly labelled alternatives.
 * CLI commands (demo:reset|trip|flight|repeat) are preserved.
 */
export const DEMO_V2 = true;
